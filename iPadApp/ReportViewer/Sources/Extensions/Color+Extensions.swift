import SwiftUI

extension Color {
    /// Brand colors for the Report Viewer app
    static let reportBlue = Color(red: 0.2, green: 0.5, blue: 0.9)
    static let reportGreen = Color(red: 0.2, green: 0.75, blue: 0.4)
    static let reportOrange = Color(red: 0.95, green: 0.6, blue: 0.2)
    static let reportRed = Color(red: 0.9, green: 0.25, blue: 0.25)

    /// Background colors
    static let cardBackground = Color(.systemBackground)
    static let screenBackground = Color(.systemGroupedBackground)
}
